/*
Packet.h - Datagram library for organizing wireless module data without
guaranteed delivery
*/

#ifndef Packet_h
#define Packet_h

#include "Arduino.h"

class Packet{
 public:
  Packet();
  setTargetId(byte id);
  setSourceId(byte id);
  setType(byte type);
  setData(byte data);
};

#endif
